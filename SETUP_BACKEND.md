# 🚀 Инструкция по установке Backend

## Ваши API ключи уже настроены!

✅ **Browserless API Key:** `2TO87BeFLtlmUIX274fd6c0a4fab3da48c2bd3d2c7b8cf67b`
✅ **ScraperAPI Key:** `d8a5c9b77c6a7bcdd64582811f9534ce`

---

## 📋 Шаг 1: Установка зависимостей

Скопируйте файл `backend/package.manual.json` в `backend/package.json`:

```bash
cp backend/package.manual.json backend/package.json
```

Затем установите зависимости:

```bash
cd backend
npm install
```

---

## 🔧 Шаг 2: TypeScript конфигурация

Скопируйте TypeScript конфиг:

```bash
cp backend/tsconfig.manual.json backend/tsconfig.json
```

---

## 🏗️ Шаг 3: Сборка проекта

```bash
npm run build
```

Это скомпилирует TypeScript код в папку `dist/`.

---

## ▶️ Шаг 4: Запуск локально (тест)

```bash
npm start
```

Сервер запустится на `http://localhost:3000`

Проверьте работу:
- Откройте `http://localhost:3000` - должна появиться информация о API
- Откройте `http://localhost:3000/health` - должен показать статус
- Откройте `http://localhost:3000/api/matches` - получит матчи с HLTV

---

## 🌐 Шаг 5: Деплой на Render.com

### 5.1. Создайте GitHub репозиторий

```bash
cd backend
git init
git add .
git commit -m "CS2 Analytics Backend"
git branch -M main
git remote add origin https://github.com/ваш-username/ваш-repo.git
git push -u origin main
```

### 5.2. Настройка на Render.com

1. Зайдите на https://render.com и войдите через GitHub
2. Нажмите **"New +"** → **"Web Service"**
3. Выберите ваш репозиторий
4. Настройте:

**Settings:**
```
Name: cs2-analytics-backend
Region: Frankfurt (или любой другой)
Branch: main
Root Directory: оставьте пустым (если backend в корне репозитория)
Runtime: Node
Build Command: npm install && npm run build
Start Command: npm start
```

**Environment Variables (добавьте эти переменные):**
```
NODE_ENV = production
PORT = 3000
BROWSERLESS_API_KEY = 2TO87BeFLtlmUIX274fd6c0a4fab3da48c2bd3d2c7b8cf67b
SCRAPER_API_KEY = d8a5c9b77c6a7bcdd64582811f9534ce
```

5. Нажмите **"Create Web Service"**

### 5.3. Получите URL

После успешного деплоя вы получите URL:
```
https://cs2-analytics-backend-xxxx.onrender.com
```

**ВАЖНО:** Сохраните этот URL! Он понадобится для подключения мобильного приложения.

---

## 📱 Шаг 6: Подключение к мобильному приложению

Откройте файл `constants/api.ts` в вашем React Native проекте и замените URL:

```typescript
const API_BASE_URL = 'https://ваш-backend-url.onrender.com';
```

Пример:
```typescript
const API_BASE_URL = 'https://cs2-analytics-backend-abc123.onrender.com';
```

---

## ✅ Проверка работы

После деплоя проверьте endpoints:

1. **Базовая информация:**
   ```
   GET https://ваш-url.onrender.com/
   ```

2. **Health check:**
   ```
   GET https://ваш-url.onrender.com/health
   ```

3. **Получить матчи:**
   ```
   GET https://ваш-url.onrender.com/api/matches
   ```

4. **Анализ матча:**
   ```
   POST https://ваш-url.onrender.com/api/matches/{match-id}/analyze
   ```

---

## 🎯 Структура файлов Backend

```
backend/
├── src/
│   ├── config/
│   │   └── index.ts              # Конфигурация (API ключи)
│   ├── scraper/
│   │   ├── browserless.ts        # Browserless интеграция
│   │   ├── scraperapi.ts         # ScraperAPI интеграция
│   │   ├── hltv.ts               # HLTV scraper
│   │   └── liquidpedia.ts        # Liquidpedia scraper
│   ├── services/
│   │   ├── analyzer.ts           # AI анализ матчей
│   │   └── cache.ts              # Кэширование
│   ├── routes/
│   │   ├── matches.ts            # API routes для матчей
│   │   └── health.ts             # Health check
│   ├── types/
│   │   └── index.ts              # TypeScript типы
│   └── index.ts                  # Entry point
├── dist/                         # Скомпилированный код (после build)
├── .env                          # Environment variables (уже настроен)
├── .env.example
├── .gitignore
├── package.json                  # (скопируйте из package.manual.json)
├── tsconfig.json                 # (скопируйте из tsconfig.manual.json)
└── render.yaml                   # Render.com конфигурация
```

---

## 🔥 Что делает Backend:

1. ✅ **Scraping HLTV:**
   - Получает live и upcoming матчи
   - Собирает статистику команд
   - Парсит последние новости

2. ✅ **Scraping Liquidpedia:**
   - Статистика игроков
   - Статистика по картам
   - История встреч (H2H)

3. ✅ **AI Анализ:**
   - Прогнозы победителя
   - Прогнозы по каждой карте
   - Вероятности и тоталы
   - Сильные и слабые стороны команд

4. ✅ **Кэширование:**
   - Матчи кэшируются на 2 минуты
   - Анализ кэшируется на 10 минут
   - Автоматическая очистка устаревших данных

5. ✅ **Обход блокировок:**
   - Browserless для JavaScript рендеринга
   - ScraperAPI для обхода блокировок

---

## ⚠️ Важные замечания

1. **Бесплатный план Render.com:**
   - Сервер засыпает через 15 минут неактивности
   - Первый запрос после пробуждения займет ~30 секунд
   - Для постоянной работы нужен платный план ($7/месяц)

2. **Лимиты API:**
   - Browserless: 1000 запросов/месяц (бесплатно)
   - ScraperAPI: 1000 запросов/месяц (бесплатно)
   - При превышении нужно апгрейдить планы

3. **Производительность:**
   - Первый запрос медленный (scraping, ~5-10 сек)
   - Последующие запросы быстрые (кэш, <100ms)
   - Рекомендуется показывать loader в приложении

---

## 🐛 Troubleshooting

**Проблема:** Backend не запускается локально
**Решение:** 
```bash
# Убедитесь что установлены зависимости
npm install

# Проверьте .env файл
cat .env

# Пересоберите
npm run build
npm start
```

**Проблема:** Ошибка "Failed to scrape"
**Решение:**
- Проверьте API ключи в `.env`
- Убедитесь что HLTV.org доступен
- Проверьте лимиты API

**Проблема:** На Render.com ошибка при деплое
**Решение:**
- Проверьте что `package.json` и `tsconfig.json` в репозитории
- Убедитесь что все Environment Variables добавлены
- Проверьте логи в Render Dashboard

---

## 📊 Мониторинг

После деплоя рекомендуется настроить мониторинг:

1. **UptimeRobot** (бесплатно): проверка доступности каждые 5 минут
2. **Render Dashboard**: просмотр логов и метрик
3. **Логирование**: все операции логируются в консоль

---

## 🎉 Готово!

После выполнения всех шагов у вас будет:

✅ Backend сервер на Render.com
✅ Реальные данные с HLTV и Liquidpedia
✅ AI анализ и прогнозы
✅ Мобильное приложение подключено к backend

Ваше приложение будет получать актуальные данные о матчах CS2 в реальном времени!
