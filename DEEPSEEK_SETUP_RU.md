# Пошаговая инструкция по настройке AI анализа через ProxyAPI + DeepSeek

## ✅ Что уже сделано автоматически:

1. ✅ Обновлен `backend/.env` с вашими ключами от ProxyAPI и PandaScore
2. ✅ Добавлена библиотека `openai` в `backend/package.manual.json`
3. ✅ Создан AI сервис (`backend/src/services/ai.ts`) для работы с DeepSeek
4. ✅ Обновлен `analyzer.ts` для использования настоящего AI анализа
5. ✅ Настроена конфигурация для работы с ProxyAPI

## 📋 Что нужно сделать вам:

### Шаг 1: Установите OpenAI SDK в backend

Откройте терминал в папке `backend` и выполните:

```bash
cd backend
npm install openai@^4.28.0
```

### Шаг 2: Пересоберите backend

```bash
npm run build
```

### Шаг 3: Задеплойте обновленный backend на Render

**Важно!** Обновите переменные окружения на Render:

1. Зайдите в Render Dashboard
2. Выберите ваш backend сервис
3. Перейдите в "Environment"
4. Добавьте/обновите:
   - `DEEPSEEK_API_KEY` = `sk-iXXarAX8uj0PlIbwj2XZSEyKihwLJiO2`
   - `DEEPSEEK_BASE_URL` = `https://api.proxyapi.ru/deepseek/v1`
   - `PANDASCORE_API_KEY` = `z61MRU06GrNSXOLx5_z5m_LkRaaRiPsd6dEfyPO_846ZN0rw00A`

5. Сохраните и нажмите "Manual Deploy" → "Deploy latest commit"

### Шаг 4: Проверьте что все работает

После деплоя:
1. Откройте приложение
2. Выберите любой матч
3. Нажмите "Analyze with AI"
4. Теперь AI будет делать настоящий анализ через DeepSeek!

## 🎯 Что теперь работает:

### AI Анализ:
- ✅ **DeepSeek через ProxyAPI** - настоящий AI анализ матчей
- ✅ **Map-by-Map Predictions** - AI анализирует каждую карту отдельно
- ✅ **Overall Prediction** - общий прогноз на матч с процентами
- ✅ **Реальные проценты победы** - не просто статистика, а AI-анализ

### Данные:
- ✅ **PandaScore** - основной источник матчей (работает!)
- ✅ **Реальные имена игроков** - берутся из PandaScore API
- ✅ **Map pool и статистика** - реальные данные команд
- ✅ **Live матчи** - отображение текущего счета и пиков карт

## 🔧 Технические детали:

### ProxyAPI + DeepSeek:
- **API Base URL**: `https://api.proxyapi.ru/deepseek/v1`
- **Модель**: `deepseek-chat`
- **Ваш токен**: `sk-iXXarAX8uj0PlIbwj2XZSEyKihwLJiO2`
- **Преимущества**: 
  - Работает из России без VPN
  - Оплата в рублях
  - Совместим с OpenAI SDK

### Как работает AI анализ:

1. **Сбор данных**:
   - Статистика команд с PandaScore
   - Статистика игроков
   - История матчей (H2H)
   - Новости о командах

2. **AI анализ каждой карты**:
   - DeepSeek получает все данные о командах
   - Анализирует статистику на конкретной карте
   - Выдает winner, probability, expected rounds

3. **Общий прогноз**:
   - На основе прогнозов по картам
   - С учетом формы команд
   - Confidence level

### Fallback система:
Если DeepSeek недоступен, система автоматически переключается на статистический анализ (старая логика).

## 💰 Стоимость использования:

- **DeepSeek через ProxyAPI**: ~0.14₽ за 1000 токенов (очень дешево!)
- **PandaScore**: 50,000 бесплатных запросов в месяц

## 🐛 Отладка:

Если что-то не работает, проверьте логи на Render:
```
[AI] DeepSeek client initialized ✅
[AI] Analyzing map Dust2 for Team A vs Team B...
[AI] Map analysis complete: Team A with 65.5%
```

## 📝 Примечания:

- HLTV и Liquidpedia больше не используются как основные источники (только fallback)
- PandaScore теперь основной источник данных
- AI анализ работает асинхронно для каждой карты
- Результаты кэшируются на 10 минут

## ❓ Если возникли проблемы:

1. Проверьте переменные окружения на Render
2. Убедитесь что `openai` установлен в backend
3. Проверьте баланс на ProxyAPI
4. Посмотрите логи на Render

Все должно работать после деплоя! 🎉
